package com.example.demo.entitys;

/**
 * Estados en los que puede estar una habitación, según el diagrama de clases.
 * Se usa un enum para que solo existan estos tres valores y no cualquier texto.
 */
public enum EstadoHabitacion {

    DISPONIBLE,
    OCUPADA,
    MANTENIMIENTO

}
